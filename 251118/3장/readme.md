# 파이썬 익스프레스 - 3장 요약

## 📚 조건문 (Conditional Statements)

### 학습 목표

- ✅ 제어문에 대하여 이해한다
- ✅ if-else 문을 이해하고 사용할 수 있다
- ✅ 관계연산자와 논리연산자를 학습한다
- ✅ 블록의 개념을 학습한다
- ✅ 중첩 if-else 문을 학습한다
- ✅ 연속 if-else 문을 학습한다

---

## 🎮 제어 구조 (Control Structures)

### 3가지 제어 구조

1. **순차 구조** (Sequential): 위에서 아래로 순서대로 실행
2. **선택 구조** (Selection): 조건에 따라 실행 경로 선택
3. **반복 구조** (Repetition): 특정 코드를 여러 번 실행

```
순차 구조        선택 구조        반복 구조
   ↓              ↓               ↓
   ↓           조건?            조건?
   ↓          ↙   ↘            ↓  ↺
   ↓        참      거짓        ↓
```

### 제어문의 종류

- **조건문**: if, if-else, elif
- **반복문**: while, for (다음 장에서 학습)

---

## 🔀 if-else 문

### 기본 구조

```python
if 조건식:
    # 조건이 참일 때 실행
    문장1
else:
    # 조건이 거짓일 때 실행
    문장2
```

### if 문만 사용 (else 생략)

```python
shipping_cost = 3000  # 기본 배송비

if price > 20000:
    shipping_cost = 0  # 조건 만족 시 배송비 무료

print("배송비 =", shipping_cost)
```

---

## 📏 블록 (Block)

### 들여쓰기의 중요성

파이썬에서는 들여쓰기로 블록을 구분합니다. (다른 언어의 중괄호 `{}` 역할)

```python
if price > 20000:
    shipping_cost = 0      # 이 줄은 if 블록에 속함
    print("무료 배송!")     # 이 줄도 if 블록에 속함
print("완료")              # 이 줄은 if 블록 밖
```

⚠️ **주의**: 들여쓰기는 보통 스페이스 4칸 또는 탭을 사용

---

## 🔍 관계 연산자 (Relational Operators)

### 관계 연산자 목록

| 연산자 | 의미 | 예시 | 결과 |
|--------|------|------|------|
| `==` | 같다 | `5 == 5` | `True` |
| `!=` | 같지 않다 | `5 != 3` | `True` |
| `>` | 크다 | `5 > 3` | `True` |
| `<` | 작다 | `5 < 3` | `False` |
| `>=` | 크거나 같다 | `5 >= 5` | `True` |
| `<=` | 작거나 같다 | `3 <= 5` | `True` |

### 부울 변수 (Boolean Variable)

```python
radius = 100
flag = (radius > 32)  # True
print(flag)  # True

expensive = (price > 20000)
if expensive:
    shipping_cost = 0
```

### 문자열 비교

```python
s1 = "Audrey Hepburn"
s2 = "Audrey Hepburn"
print(s1 == s2)  # True

s1 = "Audrey Hepburn"
s2 = "Grace Kelly"
print(s1 < s2)   # True (사전순 비교)
```

### 실수 비교 시 주의사항

```python
from math import sqrt

n = sqrt(3.0)

# 직접 비교 (비권장)
if n * n == 3.0:
    print("같다")
else:
    print("같지 않다")  # 이것이 출력됨 (부동소수점 오차)

# 오차 범위 내에서 비교 (권장)
if abs(n * n - 3.0) < 0.00001:
    print("같다")  # 이것이 출력됨
```

---

## 🎯 조건 연산자 (Ternary Operator)

### 기본 형식

```python
변수 = (참일_때_값 if 조건식 else 거짓일_때_값)
```

### 예제

```python
# 배송비 결정
shipping_cost = (0 if price >= 20000 else 3000)

# 절댓값 계산
absolute_value = (x if x > 0 else -x)

# 최댓값 계산
max_value = (x if x > y else y)

# 최솟값 계산
min_value = (x if x < y else y)
```

### 실전 예제

```python
x = int(input("첫 번째 수 = "))
y = int(input("두 번째 수 = "))

max_value = (x if x > y else y)
min_value = (y if x > y else x)

print("큰 수=", max_value, "작은 수=", min_value)
```

---

## 🔗 논리 연산자 (Logical Operators)

### 논리 연산자 종류

| 연산자 | 의미 | 예시 | 설명 |
|--------|------|------|------|
| `and` | 논리곱 (AND) | `A and B` | A와 B 모두 참일 때 참 |
| `or` | 논리합 (OR) | `A or B` | A 또는 B 중 하나라도 참이면 참 |
| `not` | 논리부정 (NOT) | `not A` | A가 거짓이면 참, 참이면 거짓 |

### 진리표

**AND 연산자**
| A | B | A and B |
|---|---|---------|
| True | True | True |
| True | False | False |
| False | True | False |
| False | False | False |

**OR 연산자**
| A | B | A or B |
|---|---|--------|
| True | True | True |
| True | False | True |
| False | True | True |
| False | False | False |

**NOT 연산자**
| A | not A |
|---|-------|
| True | False |
| False | True |


### 드모르간의 법칙 (De Morgan's Law)

복잡한 논리식을 단순화하는 법칙

```python
# 복잡한 식
if not (country == "한국" and province != "제주"):
    shipping_cost = 8000

# 단순화된 식 (드모르간의 법칙 적용)
if country != "한국" or province == "제주":
    shipping_cost = 8000
```

**법칙**
- `not (A and B)` = `(not A) or (not B)`
- `not (A or B)` = `(not A) and (not B)`

---

## 🪜 중첩 if 문 (Nested if)

### 개념

if 문 안에 또 다른 if 문이 포함된 구조

```python
if 조건1:
    if 조건2:
        # 조건1과 조건2가 모두 참일 때
        문장1
    else:
        # 조건1은 참, 조건2는 거짓일 때
        문장2
else:
    # 조건1이 거짓일 때
    문장3
```

### 예제: 국제 배송비 계산

```python
country = input("배송지(korea 또는 us): ")
price = int(input("상품의 가격: "))

if country == "korea":
    if price >= 20000:
        shipping_cost = 0
    else:
        shipping_cost = 3000
else:  # 미국
    if price >= 100000:
        shipping_cost = 0
    else:
        shipping_cost = 8000

print("배송비 =", shipping_cost)
```

---

## 🎚️ 연속 if 문 (elif)

### 기본 구조

```python
if 조건1:
    문장1
elif 조건2:
    문장2
elif 조건3:
    문장3
else:
    문장4
```

### 예제: 학점 결정

```python
score = int(input("성적을 입력하시오: "))

if score >= 90:
    print("학점 A")
elif score >= 80:
    print("학점 B")
elif score >= 70:
    print("학점 C")
elif score >= 60:
    print("학점 D")
else:
    print("학점 F")
```

**실행 결과**
```
성적을 입력하시오: 88
학점 B
```

---

## 📋 핵심 요약

### 조건문 비교

| 구조 | 사용 시기 | 예시 |
|------|-----------|------|
| **if** | 단일 조건 검사 | 배송비 무료 여부만 확인 |
| **if-else** | 2가지 경우 분기 | 짝수/홀수 판별 |
| **if-elif-else** | 3가지 이상 분기 | 학점 결정 (A, B, C, D, F) |
| **중첩 if** | 조건 안에 또 다른 조건 | 국가별 다른 배송비 계산 |

### 연산자 우선순위

1. `not`
2. `and`
3. `or`
4. 비교 연산자 (`==`, `!=`, `>`, `<`, `>=`, `<=`)

```python
# 예제
result = not True or False and True
# 계산 순서: not True → False
#           False and True → False
#           False or False → False
```

### 조건문 작성 팁

✅ **DO (권장)**
- 들여쓰기를 일관성 있게 사용
- 복잡한 조건은 변수에 저장하여 가독성 향상
- 실수 비교 시 오차 범위 고려
- 의미 있는 변수명 사용

❌ **DON'T (비권장)**
- 너무 깊은 중첩 피하기 (3단계 이상)
- 불필요한 `else` 사용 자제
- 중복 코드 작성하지 않기

---

## 🎯 연습 문제

1. **나이 확인**: 사용자의 나이를 입력받아 미성년자/성인 구분
2. **계절 판별**: 월(1-12)을 입력받아 봄/여름/가을/겨울 출력
3. **윤년 판별**: 연도를 입력받아 윤년 여부 확인
4. **최댓값 찾기**: 세 개의 수 중 가장 큰 수 찾기
5. **BMI 판정**: BMI 값에 따라 저체중/정상/과체중/비만 판정
6. **가위바위보 게임**: 컴퓨터와 가위바위보 게임
7. **계산기**: 두 수와 연산자(+,-,*,/)를 입력받아 계산
8. **비밀번호 검증**: 길이와 특수문자 포함 여부 확인

---

## ✅ 학습 점검

- [ ] if-else 문의 기본 구조 이해
- [ ] 블록과 들여쓰기의 중요성 파악
- [ ] 관계 연산자 6가지 사용법 숙지
- [ ] 논리 연산자(and, or, not) 활용
- [ ] 조건 연산자(삼항 연산자) 이해
- [ ] 중첩 if 문 작성 및 이해
- [ ] elif를 사용한 다중 분기 구현
- [ ] 실수 비교 시 주의사항 인지
- [ ] random 모듈 활용한 프로그램 작성

---

## 🔑 핵심 개념 정리

```python
# 1. 기본 if-else
if 조건:
    실행문
else:
    실행문

# 2. 다중 조건 (elif)
if 조건1:
    실행문1
elif 조건2:
    실행문2
else:
    실행문3

# 3. 중첩 if
if 조건1:
    if 조건2:
        실행문
    else:
        실행문

# 4. 논리 연산자
if 조건1 and 조건2:  # 둘 다 참
    실행문

if 조건1 or 조건2:   # 하나라도 참
    실행문

if not 조건:         # 거짓일 때
    실행문

# 5. 조건 연산자
결과 = 참값 if 조건 else 거짓값
```
