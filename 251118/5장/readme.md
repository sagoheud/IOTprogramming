# 파이썬 익스프레스 - 5장 요약

## 📚 함수 (Functions)

### 학습 목표

- ✅ 함수의 개념을 학습한다
- ✅ 함수를 작성하는 방법을 학습한다
- ✅ 함수를 호출하여 사용하는 방법을 학습한다
- ✅ 매개변수와 인수를 이해한다
- ✅ 값을 반환하는 방법을 학습한다
- ✅ 변수의 범위(지역/전역)를 이해한다

---

## 🎯 함수란?

### 함수의 정의

특정 작업을 수행하는 명령어들의 모음에 이름을 붙인 것

```python
def 함수이름(매개변수):
    실행할_코드
    return 반환값
```

### 함수의 필요성

1. **코드 재사용**: 반복되는 코드를 한 번만 작성
2. **구조화**: 프로그램을 논리적 단위로 분할
3. **유지보수**: 수정이 용이하고 오류 찾기 쉬움
4. **가독성**: 코드의 의도를 명확하게 표현

---

## 📦 코드를 묶는 3가지 방법

| 방법 | 설명 | 예시 |
|------|------|------|
| **함수** | 반복적으로 사용하는 코드를 묶음 | `def calculate()` |
| **객체** | 관련 있는 변수와 함수를 묶음 | `class Student` |
| **모듈** | 함수나 객체를 파일에 모음 | `import math` |

---

## 🔧 함수 작성과 호출

### 기본 구조

```python
# 함수 정의
def get_area(radius):
    area = 3.14 * radius ** 2
    return area

# 함수 호출
result = get_area(3)
print("반지름이 3인 원의 면적=", result)  # 28.26
```

### 함수의 구성 요소

```python
def function_name(parameter1, parameter2):  # 함수 헤더
    # 함수 몸체 (body)
    result = parameter1 + parameter2
    return result  # 반환문
```

### 함수 호출 과정

```python
def greet(name):
    print(f"안녕하세요, {name}님!")
    return

# 함수 호출
greet("홍길동")  # "안녕하세요, 홍길동님!"

# 실행 흐름:
# 1. greet("홍길동") 호출
# 2. 함수 내부 코드 실행
# 3. 함수 종료 후 호출 위치로 복귀
```

---

## 📥 매개변수와 인수

### 용어 정리

```python
def add(x, y):      # x, y: 매개변수 (parameter)
    return x + y

result = add(10, 20)  # 10, 20: 인수 (argument)
```

| 용어 | 설명 | 위치 |
|------|------|------|
| **매개변수** (Parameter) | 함수 정의 시 사용하는 변수 | 함수 정의부 |
| **인수** (Argument) | 함수 호출 시 전달하는 실제 값 | 함수 호출부 |

### 여러 개의 매개변수

```python
def get_sum(start, end):
    sum = 0
    for i in range(start, end + 1):
        sum += i
    return sum

x = get_sum(1, 10)   # 55
y = get_sum(1, 20)   # 210
```

---

## 🔄 값 반환하기

### return 문

```python
# 값을 반환하는 함수
def add(a, b):
    return a + b

result = add(5, 3)  # 8

# 값을 반환하지 않는 함수
def print_message():
    print("안녕하세요")
    # return이 없으면 None 반환
```

### 모든 경우에 값 반환하기

```python
# 나쁜 예
def get_area(radius):
    if radius > 0:
        return 3.14 * radius ** 2
    # radius <= 0일 때 아무것도 반환하지 않음

# 좋은 예
def get_area(radius):
    if radius > 0:
        return 3.14 * radius ** 2
    else:
        return 0
```

### 여러 개의 값 반환

```python
# 여러 값을 동시에 반환 (튜플 사용)
def get_info():
    name = "홍길동"
    age = 20
    return name, age

# 반환된 값 받기
student_name, student_age = get_info()
print(student_name, student_age)  # 홍길동 20

# 또 다른 예
def addsub(x, y):
    return x + y, x - y

sum_result, sub_result = addsub(10, 3)
print(sum_result, sub_result)  # 13 7
```

---

## 🎨 다양한 매개변수 유형

### 1. 기본 매개변수

```python
def greet(name):
    print(f"안녕하세요, {name}님!")

greet("김철수")  # 안녕하세요, 김철수님!
```

### 2. 디폴트 인수

```python
def greet(name, msg="별일없죠?"):
    print(f"안녕, {name}, {msg}")

greet("영희")              # 안녕, 영희, 별일없죠?
greet("철수", "잘 지내?")   # 안녕, 철수, 잘 지내?
```

### 3. 키워드 인수

```python
def introduce(name, age, city):
    print(f"{name}, {age}살, {city} 거주")

# 위치 인수
introduce("홍길동", 25, "서울")

# 키워드 인수
introduce(name="홍길동", age=25, city="서울")
introduce(city="서울", name="홍길동", age=25)  # 순서 무관

# 혼합 사용 (위치 인수가 먼저)
introduce("홍길동", age=25, city="서울")
```

### 4. 가변 인수 (*args)

```python
# 여러 개의 인수를 튜플로 받기
def add(*numbers):
    sum = 0
    for n in numbers:
        sum += n
    return sum

print(add(10, 20))           # 30
print(add(10, 20, 30))       # 60
print(add(1, 2, 3, 4, 5))    # 15
```

### 5. 가변 키워드 인수 (**kwargs)

```python
# 키워드 인수를 딕셔너리로 받기
def print_info(**kwargs):
    for key, value in kwargs.items():
        print(f"{key}: {value}")

print_info(name="홍길동", age=25, city="서울")
# 출력:
# name: 홍길동
# age: 25
# city: 서울
```

### 매개변수 유형 정리

| 유형 | 형식 | 예시 | 설명 |
|------|------|------|------|
| 일반 | `def f(x)` | `f(10)` | 기본 매개변수 |
| 디폴트 | `def f(x=10)` | `f()` 또는 `f(5)` | 기본값 제공 |
| 키워드 | `def f(x, y)` | `f(x=1, y=2)` | 이름으로 지정 |
| 가변 | `def f(*args)` | `f(1, 2, 3)` | 개수 제한 없음 |
| 가변 키워드 | `def f(**kwargs)` | `f(a=1, b=2)` | 키워드 개수 무제한 |

---

## 📦 언패킹 (Unpacking)

### 리스트 언패킹

```python
# * 연산자로 언패킹
alist = [1, 2, 3]
print(*alist)  # 1 2 3
print(alist)   # [1, 2, 3]

# 함수에 언패킹된 값 전달
def sum(a, b, c):
    return a + b + c

numbers = [1, 2, 3]
result = sum(*numbers)  # sum(1, 2, 3)과 동일
print(result)  # 6
```

### 딕셔너리 언패킹

```python
def greet(name, age):
    print(f"{name}님, {age}살이시군요!")

info = {"name": "홍길동", "age": 25}
greet(**info)  # greet(name="홍길동", age=25)와 동일
```

---

## 🔍 변수의 범위 (Scope)

### 지역 변수 (Local Variable)

```python
def myfunc():
    x = 100  # 지역 변수
    print(x)

myfunc()  # 100
# print(x)  # 오류! 함수 밖에서는 접근 불가
```

### 전역 변수 (Global Variable)

```python
gx = 100  # 전역 변수

def myfunc():
    print(gx)  # 전역 변수 읽기 가능

myfunc()  # 100
print(gx)  # 100
```

### 같은 이름의 지역 변수

```python
def func1():
    x = 100
    print(x)  # 100

def func2():
    x = 200
    print(x)  # 200

func1()
func2()
# 각 함수의 x는 독립적
```

### 전역 변수 수정하기

```python
# 잘못된 방법
gx = 100

def myfunc():
    gx = 200  # 새로운 지역 변수 생성!
    print(gx)  # 200

myfunc()
print(gx)  # 100 (전역 변수는 변경되지 않음)
```

```python
# 올바른 방법
gx = 100

def myfunc():
    global gx  # 전역 변수 사용 선언
    gx = 200
    print(gx)  # 200

myfunc()
print(gx)  # 200 (전역 변수 변경됨)
```

### 변수 범위 정리

| 변수 종류 | 선언 위치 | 접근 범위 | 수명 |
|-----------|-----------|-----------|------|
| **지역 변수** | 함수 내부 | 함수 내부만 | 함수 실행 중 |
| **전역 변수** | 함수 외부 | 모든 곳 | 프로그램 전체 |

---

## 🔄 순환 호출 (Recursion)

### 순환이란?

함수가 자기 자신을 호출하는 프로그래밍 기법

### 팩토리얼 예제

```python
def factorial(n):
    if n == 1:
        return 1
    else:
        return n * factorial(n - 1)

print(factorial(5))  # 120

# 실행 과정:
# factorial(5) = 5 * factorial(4)
# factorial(4) = 4 * factorial(3)
# factorial(3) = 3 * factorial(2)
# factorial(2) = 2 * factorial(1)
# factorial(1) = 1
# 결과: 5 * 4 * 3 * 2 * 1 = 120
```

### 순환의 구조

```python
def recursive_function(parameter):
    if 종료_조건:        # 기저 조건 (Base Case)
        return 값
    else:
        return recursive_function(변경된_parameter)  # 순환 호출
```

⚠️ **주의**: 종료 조건이 없으면 무한 순환 발생!

---


---

## 📝 함수 작성 가이드

### ✅ 좋은 함수의 특징

1. **단일 책임**: 하나의 함수는 하나의 작업만
2. **명확한 이름**: 함수의 역할을 알 수 있는 이름
3. **적절한 길이**: 보통 20-30줄 이내
4. **문서화**: 함수의 역할과 매개변수 설명

```python
def calculate_circle_area(radius):
    """
    원의 면적을 계산합니다.
    
    Args:
        radius (float): 원의 반지름
    
    Returns:
        float: 원의 면적
    """
    if radius <= 0:
        return 0
    return 3.14 * radius ** 2
```

### 함수 설계 팁

| 원칙 | 설명 | 예시 |
|------|------|------|
| **명명 규칙** | 동사로 시작하는 명확한 이름 | `get_area()`, `calculate_sum()` |
| **매개변수** | 3개 이하 권장 | `func(x, y, z)` ❌ → 구조체 사용 |
| **반환값** | 일관된 타입 반환 | 항상 숫자 또는 항상 None |
| **부작용 최소화** | 전역 변수 수정 자제 | `global` 키워드 최소 사용 |

---

## 🔄 함수 호출 순서

### 순서의 중요성

```python
# 오류 발생
result = get_area(3)  # get_area가 아직 정의되지 않음!

def get_area(radius):
    return 3.14 * radius ** 2
```

```python
# 올바른 순서
def get_area(radius):
    return 3.14 * radius ** 2

result = get_area(3)  # OK
```

### 함수 내에서의 순서

```python
# 함수 안에서는 순서 무관
def main():
    result = get_area(3)  # OK (실행 시점에 정의됨)
    print(result)

def get_area(radius):
    return 3.14 * radius ** 2

main()  # 호출 시점에 모든 함수가 정의됨
```

---

## 📊 함수 vs 일반 코드 비교

### 함수 없이 작성

```python
# 원의 면적 계산 (중복 코드)
radius1 = 5
area1 = 3.14 * radius1 ** 2
print(f"면적1: {area1}")

radius2 = 10
area2 = 3.14 * radius2 ** 2
print(f"면적2: {area2}")

radius3 = 15
area3 = 3.14 * radius3 ** 2
print(f"면적3: {area3}")
```

### 함수로 작성

```python
def get_area(radius):
    return 3.14 * radius ** 2

print(f"면적1: {get_area(5)}")
print(f"면적2: {get_area(10)}")
print(f"면적3: {get_area(15)}")
```

💡 **장점**: 코드 중복 제거, 수정 용이, 가독성 향상

---

## 🎯 함수 사용의 이유

| 이유 | 설명 | 효과 |
|------|------|------|
| **재사용성** | 한 번 작성한 코드를 여러 번 사용 | 개발 시간 단축 |
| **모듈화** | 복잡한 문제를 작은 단위로 분해 | 문제 해결 용이 |
| **유지보수** | 수정이 필요할 때 한 곳만 변경 | 오류 감소 |
| **가독성** | 코드의 의도를 명확하게 표현 | 협업 효율 증가 |
| **테스트** | 개별 함수 단위로 테스트 가능 | 품질 향상 |

---

## 📋 핵심 요약

### 함수 기본 구조

```python
def function_name(parameter1, parameter2):
    """함수 설명 (docstring)"""
    # 함수 본문
    result = parameter1 + parameter2
    return result
```

### 매개변수 유형

```python
# 1. 일반 매개변수
def func(x, y):
    return x + y

# 2. 디폴트 매개변수
def func(x, y=10):
    return x + y

# 3. 가변 매개변수
def func(*args):
    return sum(args)

# 4. 키워드 가변 매개변수
def func(**kwargs):
    return kwargs
```

### 변수 범위

```python
x = 100  # 전역 변수

def func():
    y = 200  # 지역 변수
    global x  # 전역 변수 수정
    x = 300
```

---

## 🎯 연습 문제

1. **최댓값 함수**: 두 수 중 큰 수를 반환하는 함수
2. **평균 계산**: 가변 인수로 여러 수의 평균 계산
3. **소수 판별**: 숫자가 소수인지 판별하는 함수
4. **문자열 역순**: 문자열을 역순으로 반환하는 함수
5. **거듭제곱**: 순환 호출로 x^n 계산
6. **최대공약수**: 유클리드 호제법으로 GCD 계산
7. **피보나치**: 순환 호출로 n번째 피보나치 수
8. **회문 판별**: 문자열이 회문인지 확인
9. **리스트 합계**: 리스트의 모든 요소 합 계산
10. **온도 변환**: 섭씨/화씨 상호 변환 함수

---

## ✅ 학습 점검

- [ ] 함수의 개념과 필요성 이해
- [ ] 함수 정의와 호출 방법 숙지
- [ ] 매개변수와 인수의 차이 이해
- [ ] 디폴트/키워드/가변 인수 활용
- [ ] return 문으로 값 반환하기
- [ ] 여러 값 동시에 반환하기
- [ ] 지역 변수와 전역 변수 구분
- [ ] global 키워드 사용법
- [ ] 순환 호출 개념 이해
- [ ] 함수로 프로그램 구조화하기

---

## 🔑 핵심 개념 카드

### 함수의 4대 요소

1. **이름**: 함수를 식별하는 이름
2. **매개변수**: 입력 데이터를 받는 변수
3. **본문**: 실행할 코드
4. **반환값**: 결과 데이터

```python
def 이름(매개변수):  # 1, 2
    본문            # 3
    return 반환값   # 4
```

### Pass 문

```python
def future_function():
    pass  # 나중에 구현할 함수
```

---
